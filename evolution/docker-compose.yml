version: '3.8'

services:
  evolution:
    build: .
    ports:
      - "8080:8080"
    environment:
      - AUTHENTICATION_API_KEY=procasa2025evolutionclaveultra
      - SERVER_URL=https://procasa-chatbot.onrender.com
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 5